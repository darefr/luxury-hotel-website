# Neon PostgreSQL connection string (server-side only)
DATABASE_URL=

# Better Auth secret (min 32 chars). Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Optional: set only if you have a stable canonical auth URL.
# BETTER_AUTH_URL=
